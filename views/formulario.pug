doctype html
head
  meta(charset='UTF-8')
  meta(http-equiv='X-UA-Compatible' content='IE=edge')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  title Formulario de carga
h1 Formulario de carga de productos
form#productForm(action='/productos' method='post')
  label Titulo
  input(name='title')
  label Precio
  input(name='price')
  label Url Imagen
  input(name='thumbnail')
  button Enviar
form#productForm(action='/productos' method='get')
  button Consultar Productos